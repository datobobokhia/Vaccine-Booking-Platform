<%= simple_form_for [:admin, district] do |f| %>
<%= f.association :city %>
<%= f.input :name %>
<%= f.input :code %>
<%= f.input :active %>
<%= f.button :submit, class: 'btn btn-primary' %>
<% end %>